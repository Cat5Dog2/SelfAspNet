@model IEnumerable<SelfAspNet.Models.Book>

<h1>タイトルに「JavaScript」を含む書籍一覧</h1>
<hr>
@if(Model?.Any() == true)
{
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.Title（@item.Price 円）</li>
        }
    </ul>
}
else
{
    <p>該当する書籍はありませんでした。</p>
}